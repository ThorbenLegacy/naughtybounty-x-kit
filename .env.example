# Kopieren nach .env.local und Werte eintragen
# Postet als @LucaBrandblue (Promo für NaughtyBounty)

# OAuth 2.0 (empfohlen — braucht media.write für Bilder)
X_CLIENT_ID=
X_CLIENT_SECRET=
X_OAUTH2_ACCESS_TOKEN=
X_OAUTH2_REFRESH_TOKEN=

# OAuth 1.0a (Alternative für Media-Upload, falls OAuth2 nicht geht)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# Dashboard-Login (umbra / zero / shade — gemeinsames Passwort)
# Standard ohne Setzen: nb-umbra-zero-shade — bitte in Produktion ändern!
DASHBOARD_PASSWORD=
# Optional: Session-Signatur (zufälliger String)
# DASHBOARD_SESSION_SECRET=
# Lokal ohne Login: DASHBOARD_AUTH_DISABLED=1

# Scheduler → Dashboard (Railway Private Network, Option C)
# Nur am Scheduler setzen — Dashboard-URL intern (nicht die öffentliche URL nötig)
# DASHBOARD_INTERNAL_URL=http://naughtybounty-x-kit.railway.internal:8080
# Optional eigener Scheduler-Key; sonst wird DASHBOARD_PASSWORD genutzt
# SCHEDULER_SECRET=

# Bot: ab welchem Post weitermachen (0 = erster Post erledigt → nächster ist #2)
BOT_LAST_INDEX=0
